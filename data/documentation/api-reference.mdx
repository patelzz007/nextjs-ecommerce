---
title: "API Reference"
description: "Complete API reference documentation"
order: 2
icon: "book"
category: "reference"
---

# API Reference

Complete reference for all API endpoints, request parameters, and response formats.

## Products API

### List Products

Retrieve a paginated list of products.

\`\`\`http
GET /v1/products
\`\`\`

**Query Parameters:**

| Parameter | Type | Description |
|-----------|------|-------------|
| `limit` | integer | Number of items to return (default: 20, max: 100) |
| `offset` | integer | Number of items to skip |
| `status` | string | Filter by status: `draft`, `published` |
| `category` | string | Filter by category |

**Example Request:**

\`\`\`javascript
const response = await fetch('https://api.yourcompany.com/v1/products?limit=10&status=published', {
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY'
  }
})

const data = await response.json()
\`\`\`

\`\`\`python
import requests

response = requests.get(
    'https://api.yourcompany.com/v1/products',
    params={'limit': 10, 'status': 'published'},
    headers={'Authorization': 'Bearer YOUR_API_KEY'}
)

data = response.json()
\`\`\`

**Response:**

\`\`\`json
{
  "data": [
    {
      "id": "prod_123",
      "name": "Premium Headphones",
      "price": 19999,
      "status": "published",
      "created_at": "2024-01-15T10:30:00Z"
    }
  ],
  "meta": {
    "total": 150,
    "limit": 10,
    "offset": 0
  }
}
\`\`\`

### Create Product

Create a new product in your store.

\`\`\`http
POST /v1/products
\`\`\`

**Request Body:**

\`\`\`json
{
  "name": "New Product",
  "description": "Product description",
  "price": 9999,
  "status": "draft",
  "category": "electronics"
}
\`\`\`

## Orders API

### List Orders

Retrieve a list of orders.

\`\`\`http
GET /v1/orders
\`\`\`

**Example Request:**

\`\`\`bash
curl https://api.yourcompany.com/v1/orders \
  -H "Authorization: Bearer YOUR_API_KEY"
\`\`\`
